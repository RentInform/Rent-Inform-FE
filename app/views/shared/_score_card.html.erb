    <div class='shadow-md border border-gray-100 p-3 rounded-md flex-1 flex flex-col justify-center items-center gap-4' style="aspect-ratio: 1/1;">
      <p class='text-sm font-bold leading-none'>
        <%= title %>
      </p>
      <div class='w-16 h-16 rounded-full flex justify-center items-center' style='background: radial-gradient(closest-side, white 79%, transparent 80% 100%),
      conic-gradient(<%= score.to_i >= 75 ? 'green' : score.to_i >= 50 ? 'orange' : 'red' %> <%= score %>%, <%= score.to_i >= 75 ? 'lightgreen' : score.to_i >= 50 ? 'bisque' : 'pink' %> 0)' >
        <p class='justify-center'>
          <%= score %>
        </p>
      </div>
      <a class='text-xs text-blue-700 underline hover:text-blue-900' href=url_learn_more>Learn more</a>
    </div>